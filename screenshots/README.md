The screenshots shows the following

1. docker ps showing all the running containers in the pod.
2. wetty browser terminal emulator giving access inside the pods and netstat -plant showing the collapsed/shared network namespace.
3. The app itself
4. Marathon request. The zip file contains the compose file.
